# ConfigTool Usage

Demonstrates @senzing/configtool for offline configuration editing. No Senzing runtime needed — configtool works with pure JSON.

## Prerequisites

- A Senzing config JSON file (from `configManager.createConfig()` or a saved file)

## Run

```bash
npm install
npm start -- <path-to-config.json>
```
