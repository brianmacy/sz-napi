# Electron Worker Thread Pattern

Demonstrates how to use @senzing/sdk with Node.js worker_threads to keep the main thread responsive in an Electron application.

## Prerequisites

- Senzing runtime installed
- `DYLD_LIBRARY_PATH` (macOS) or `LD_LIBRARY_PATH` (Linux) set to the Senzing lib directory

## Run

```bash
npm install
npm start
```
