## Summary

Brief description of the changes.

## Changes

-

## Testing

- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo fmt --workspace --check` passes
- [ ] ConfigTool tests pass (`cd packages/configtool && npx vitest run`)
- [ ] SDK tests pass (`cd packages/sdk && npx vitest run`) (if applicable)

## Related Issues

Closes #
